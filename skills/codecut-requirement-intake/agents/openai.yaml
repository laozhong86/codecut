interface:
  display_name: Codecut Requirement Intake
  short_description: Confirms Codecut editing execution fields one question at a time.
  default_prompt: Use $codecut-requirement-intake before any new Codecut creative editing job. Do not run Codecut executor mutation commands until requirement intake passes. Ask one missing field per turn and use Codex request_user_input when available.
