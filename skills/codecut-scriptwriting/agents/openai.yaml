interface:
  display_name: "CodeCut Scriptwriting"
  short_description: "Draft titles, voiceover scripts, and de-AI copy."
  default_prompt: "Use $codecut-scriptwriting when a CodeCut job needs hooks, voiceover scripts, spoken-word drafts, or De-AI rewriting before editing or timeline planning. Title-only requests belong to $codecut-title-generation first; when a job mixes title generation with voiceover script or De-AI work, consume the TitleGenerationBrief and keep this skill focused on spoken copy and rewrite lanes."
